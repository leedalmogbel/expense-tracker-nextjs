# Supabase (optional – enables "Save to Supabase" + Google sign-in)
# Get these from your Supabase project: Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# For Google sign-in: enable Google in Supabase Auth → Providers, add your Google Client ID/Secret.
# In Supabase Auth → URL Configuration, add redirect URL: https://your-domain.com/auth/callback
# (e.g. http://localhost:3000/auth/callback for local dev)
